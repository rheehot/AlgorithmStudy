// 소수판별 알고리즘
/*
#include <stdio.h>

int main(void) {
	int n;
	bool is_prime = true;
	scanf_s("%d", &n);

	for (int i = 2; i < n; i++) {
		if (n% i == 0){
			is_prime = false;
		}
	}
	if (is_prime == true){
		printf("%d is prime", n);
	}
	else {
		printf("%d is not prime", n);
	}
}
*/
